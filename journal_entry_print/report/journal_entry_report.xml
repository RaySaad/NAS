<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- PDF Report Action -->
    <record id="action_report_journal_entry" model="ir.actions.report">
        <field name="name">Journal Entry Voucher</field>
        <field name="model">account.move</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">journal_entry_print.report_journal_entry_voucher</field>
        <field name="report_file">journal_entry_print.report_journal_entry_voucher</field>
        <field name="print_report_name">'JV_%s' % (object.name or 'Draft').replace('/', '_')</field>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_type">report</field>
    </record>
</odoo>
